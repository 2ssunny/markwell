[pytest]
testpaths = tests
addopts = -q
markers =
    real_paths: bypass the autouse user-data isolation guard in conftest.py so a
        test can exercise markwell.paths' real directory-resolution logic (e.g.
        legacy-folder migration) against a faked LOCALAPPDATA instead of a plain
        tmp_path. Only test_migration.py should need this.
